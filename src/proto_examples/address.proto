syntax = "proto3";

package demo; // Requried to generate valid code.

// Always import protos with a full path relative to the WORKSPACE file.
import "src/proto_examples/zip_code.proto";

message Address {
  string city = 1;
  ZipCode zip_code = 2;
}

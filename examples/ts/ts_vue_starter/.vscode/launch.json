{
    "configurations": [
        {
            "name": "Launch Chrome against localhost",
            "request": "launch",
            "type": "chrome",
            "url": "http://localhost:8088",
            "webRoot": "${workspaceFolder}"
        }
    ],
    "version": "0.2.0"
}
